import HookScene from '../components/HookScene'
import ProblemScene from '../components/ProblemScene'
import ValueScene from '../components/ValueScene'
import ContrastScene from '../components/ContrastScene'

export const sceneRegistry = {
  hook: HookScene,
  problem: ProblemScene,
  value: ValueScene,
  contrast: ContrastScene,
}